<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>商用級スロットマシン</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .game-container {
            width: 900px;
            background: linear-gradient(180deg, #2a2a3e 0%, #1a1a2e 100%);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.8);
            border: 3px solid #3a3a4e;
        }

        .display-panel {
            display: flex;
            justify-content: space-around;
            margin-bottom: 20px;
            padding: 15px;
            background: #000;
            border-radius: 10px;
        }

        .display-item {
            text-align: center;
        }

        .display-label {
            font-size: 12px;
            color: #ff6b35;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .display-value {
            font-size: 32px;
            color: #00ff88;
            font-family: 'Courier New', monospace;
            font-weight: bold;
            text-shadow: 0 0 10px #00ff88;
        }

        #gameCanvas {
            display: block;
            width: 100%;
            height: 400px;
            background: #0a0a0a;
            border-radius: 15px;
            border: 4px solid #2a2a2a;
            margin-bottom: 20px;
            box-shadow: inset 0 8px 16px rgba(0, 0, 0, 0.9);
        }

        .control-panel {
            display: flex;
            gap: 15px;
            justify-content: center;
            padding: 20px;
            background: linear-gradient(180deg, #2a2a3e 0%, #1a1a2e 100%);
            border-radius: 10px;
        }

        .btn {
            padding: 15px 30px;
            border: none;
            border-radius: 10px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.2s;
            box-shadow: 0 4px 0 rgba(0, 0, 0, 0.3);
            position: relative;
            top: 0;
        }

        .btn:active:not(:disabled) {
            top: 3px;
            box-shadow: 0 1px 0 rgba(0, 0, 0, 0.3);
        }

        .btn:disabled {
            opacity: 0.4;
            cursor: not-allowed;
        }

        .btn-start {
            background: linear-gradient(180deg, #ff4757 0%, #cf2737 100%);
            color: white;
            font-size: 22px;
            padding: 20px 40px;
        }

        .btn-stop {
            background: linear-gradient(180deg, #ffd700 0%, #cfb700 100%);
            color: #000;
            min-width: 100px;
        }

        .debug-panel {
            margin-top: 20px;
            padding: 15px;
            background: rgba(0, 0, 0, 0.8);
            border-radius: 8px;
            font-family: 'Courier New', monospace;
            font-size: 12px;
            color: #0f0;
            max-height: 200px;
            overflow-y: auto;
        }

        .debug-controls {
            display: flex;
            gap: 10px;
            margin-bottom: 10px;
            flex-wrap: wrap;
        }

        .debug-controls button,
        .debug-controls select {
            padding: 5px 10px;
            font-size: 11px;
        }
    </style>
</head>
<body>
    <div class="game-container">
        <div class="display-panel">
            <div class="display-item">
                <div class="display-label">CREDIT</div>
                <div class="display-value" id="creditDisplay">50</div>
            </div>
            <div class="display-item">
                <div class="display-label">BET</div>
                <div class="display-value" id="betDisplay">0</div>
            </div>
            <div class="display-item">
                <div class="display-label">PAYS</div>
                <div class="display-value" id="paysDisplay">0</div>
            </div>
        </div>

        <canvas id="gameCanvas"></canvas>

        <div class="control-panel">
            <button class="btn btn-start" id="startBtn">START</button>
            <button class="btn btn-stop" id="stopBtn1">STOP 1</button>
            <button class="btn btn-stop" id="stopBtn2">STOP 2</button>
            <button class="btn btn-stop" id="stopBtn3">STOP 3</button>
        </div>

        <div class="debug-panel">
            <div class="debug-controls">
                <button id="debugToggle">Debug ON/OFF</button>
                <select id="settingSelect">
                    <option value="1">設定1</option>
                    <option value="2">設定2</option>
                    <option value="3" selected>設定3</option>
                    <option value="4">設定4</option>
                    <option value="5">設定5</option>
                    <option value="6">設定6</option>
                </select>
                <button id="forceBonusBtn">強制BONUS</button>
                <button id="resetBtn">RESET</button>
            </div>
            <div id="debugOutput"></div>
        </div>
    </div>

    <!-- Core -->
    <script src="src/core/EventBus.js"></script>
    <script src="src/core/Clock.js"></script>
    <script src="src/core/Logger.js"></script>
    
    <!-- Infrastructure -->
    <script src="src/infrastructure/ConfigProvider.js"></script>
    <script src="src/infrastructure/LocalStorageRepo.js"></script>
    
    <!-- Domain -->
    <script src="src/domain/StateMachine.js"></script>
    <script src="src/domain/CreditManager.js"></script>
    <script src="src/domain/RNGService.js"></script>
    <script src="src/domain/OutcomeResolver.js"></script>
    <script src="src/domain/ReelController.js"></script>
    <script src="src/domain/StopPlanner.js"></script>
    <script src="src/domain/GameSession.js"></script>
    
    <!-- Presentation -->
    <script src="src/presentation/CanvasRenderer.js"></script>
    <script src="src/presentation/UILayer.js"></script>
    <script src="src/presentation/EffectLayer.js"></script>
    <script src="src/presentation/SoundController.js"></script>
    
    <!-- Core Kernel -->
    <script src="src/core/GameKernel.js"></script>
    
    <!-- Entry Point -->
    <script src="src/main.js"></script>
</body>
</html>
